import 'package:flutter/material.dart';

class MyGridMenu extends StatelessWidget {
  const MyGridMenu({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
